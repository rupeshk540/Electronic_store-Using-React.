import Base from "../components/Base";

function Services(){
    return (
        <Base
        title="Services we provide"
        description="In this page we will discuss about services we provide"
        buttonEnabled={true}
        buttonLink="/"
        buttonType="warning"
        buttonText="Home"
        >
        <h1>this is feature page</h1>
        </Base>
    )
}

export default Services;